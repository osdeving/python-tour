# Deploy FastAPI com Uvicorn
# uvicorn fastapi_exemplo:app --host 0.0.0.0 --port 80
# Dockerfile exemplo:
# FROM python:3.11
# WORKDIR /app
# COPY . .
# RUN pip install fastapi uvicorn
# CMD ["uvicorn", "fastapi_exemplo:app", "--host", "0.0.0.0", "--port", "80"]
